# Comprehensive Site Re-Audit (2026-03-12)

## Scope

Full re-audit run across build, lint, types, unit tests, E2E tests, accessibility, Supabase catalog integrity, Nhost parity, slug/ID integrity, and external asset host policy.

## Results Matrix

- `npm run lint`: pass
- `npm run build`: pass
- `npx tsc --noEmit`: fail
  - `tests/3d-model.test.tsx` references missing module `../components/3DViewer`
- `npm test`: fail
  - 4 suites passed, 1 failed (`tests/3d-model.test.tsx` missing `../components/3DViewer`)
  - warning signal in passing suite: `tests/images.test.ts` reports 10 products with no images
- `npm run test:e2e:nav`: fail (2/5 timed out)
  - desktop mega menu category-group navigation timeout
  - quote cart badge flow timeout on `/products/seating`
- `npm run test:e2e:filters`: fail (3/3)
  - route load/interaction timeouts and filtered breadcrumb assertion failure
- `npm run test:e2e:stats-consistency`: fail (1/1 timeout on `/`)
- `npm run test:a11y`: pass (6/6)
- `npm run audit:supabase:catalog`: pass
  - `products=145`, `blankSlugs=0`, `duplicateSlugs=0`
- `npm run audit:nhost:backup`: pass with known gap
  - `tableDiffs=0`, `graphqlFailures=1` (`product_slug_aliases` not exposed on `query_root`)
- `npm run audit:slug-id`: pass with findings
  - `products=145`, `aliasCoverageMissing=4`, `canonicalConflicts=2`
- `npm run assets:audit:thirdparty`: pass
  - `blocked_db_rows=0`, `blocked_code_matches=0`
- `npm run audit:products:quality`: fail
  - stale import references removed module `../lib/catalog.ts`

## Primary Blockers

- Test/type drift:
  - `tests/3d-model.test.tsx` still targets deleted `components/3DViewer.tsx`
  - blocks `npm test` and `npx tsc --noEmit`
- E2E stability:
  - navigation, dynamic-filters, and stats-consistency suites show repeatable 30s timeouts and route-load instability
- Audit tooling drift:
  - `scripts/audit-product-quality.ts` still depends on removed legacy modules (`lib/catalog.ts`, `lib/productSpecSchema.ts`)
- Platform gap (already tracked):
  - Nhost GraphQL alias-table exposure (`product_slug_aliases` on `query_root`)

## Notes

- This re-audit does not delete files and preserves current recovery constraints.
- The currently locked platform follow-up remains valid: fix GraphQL alias-table exposure and re-run parity audit until `graphqlFailures=0`.
