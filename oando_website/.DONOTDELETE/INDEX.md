# DO NOT DELETE Archive

This folder is a safety copy of critical rollout documents.

## Contents
- `walkthroughs/` - database and Cloudflare execution walkthroughs
- `configs/` - config snapshots (`next.config.js`, `.env.example`, `tsconfig.json`)
- `keys/` - local secret-bearing files snapshot (`.env.local`, `.env.example`, wrangler account context)
- `documents/` - master index, README, slug policy and plan docs
- `reports/` - audits and reconcile reports
- `sql/` - migration and plan SQL copies

Source folder: `oando_website`
