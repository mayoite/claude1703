# Keys Snapshot

This folder stores local key/config snapshots for backup before encryption.

Current files:
- `.env.local`
- `.env.example`
- `wrangler-account.json`

Notes:
- Keep this folder local-only until encrypted.
- Rotate any credential that was ever shared in plaintext.
