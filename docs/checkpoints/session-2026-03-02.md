# Session Checkpoint - 2026-03-02

## User Requirements (Locked)
- Do not hardcode data.
- Redo and use real Supabase-backed data.
- Keep backup path for data safety.
- Work one task at a time, with direct proof.
- Keep this same thread context active.

## Current Focus
- Active file: `app/products/[category]/[slug]/ProductViewer.tsx`

## Confirmed Work Done
- Added backup script: `scripts/backup_supabase.ts`
- Added npm script entry for backup in `package.json` (`supabase:backup`)
- Added scheduled GitHub Action: `.github/workflows/supabase-backup.yml` (daily cron + manual run)
- Hardened backup script behavior:
  - Retries transient Cloudflare/network failures.
  - Skips missing tables instead of failing entire run.
  - Exits non-zero only on real backup errors.
- Local verified backup run:
  - Run ID: `20260302-045500`
  - Output dir: `backups/supabase/20260302-045500`
  - `tables_ok=3`, `tables_skipped=5`, `tables_error=0`
  - Backed up: `categories` (6), `products` (145), `business_stats_current` (1)
- Added Nhost env wiring without code hardcoding:
  - Local `.env.local` keys set: `NHOST_GRAPHQL_ENDPOINT`, `NHOST_SERVICE_ROLE_KEY`
  - GitHub Action env placeholders added (reads from secrets): `NHOST_GRAPHQL_ENDPOINT`, `NHOST_SERVICE_ROLE_KEY`

## Known Open Items
- Local cache clear was attempted once, but one delete command was blocked by policy.
- Supabase integration and product technical details still require final end-to-end validation.
- GitHub CLI auth is currently invalid (`gh auth status` failed), so repo secrets could not be set automatically from CLI in this session.

## Working Protocol From Here
1. Do one exact task only.
2. Show exact command/output proof.
3. No speculative claims.
4. No hardcoded fallback data.
